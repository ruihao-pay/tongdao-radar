{
  "type": "module",
  "scripts": {
    "check": "node --check src/worker.js && npm test && npx wrangler deploy --dry-run",
    "deploy": "npx wrangler deploy",
    "whoami": "npx wrangler whoami",
    "login": "npx wrangler login",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
